"use client";

// Timezone is scheduling-sensitive, so the app no longer writes browser-detected
// values automatically. Users can save their preferred timezone from Settings.
export function TimezoneSync() {
  return null;
}
